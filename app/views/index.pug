extends layout
block content
    h1 #{heading}
    p first paragraph xxx
    p Abdul testing comment
    p this is a landing page
    p check out the dashboard
    a(href="http://dashboard") payroll
    p now lets loop 
    each item in data  
        p #{item}